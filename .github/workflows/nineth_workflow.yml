name: nineth_workflow

on: [push]
      
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Contects and Expressions
        id: context-expression
        run: |
          echo ${{ toJson(github)}}"
          echo ${{ toJson(runner)}}"
          echo ${{ toJson(job)}}"
          echo ${{ toJson(steps)}}"