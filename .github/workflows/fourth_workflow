name: Fourth_workflow

on: 
  workflow_run:
    workflows: [Third_workflow]
    types: 
      - completed

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List Files
        run: echo "Hello"