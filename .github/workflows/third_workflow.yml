name: Third_workflow

on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List Files
        run: ls -a
      - uses: actions/checkout@v6
      - name: List Files
        run: ls -a