name: Third_workflow

on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display text
        run: echo "Some Text"
      - name: Command in line
        run: |
          pwd
          ls -a
  