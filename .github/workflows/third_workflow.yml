name: Third_workflow

on: [push]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display text
        run: echo "Some Text"
      - name: List Files
        run: ls -a
      - users: actions/checkout@v4
      - name: List Files
        run: ls -a